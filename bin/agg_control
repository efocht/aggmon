#!/usr/bin/env python2
import os
import sys

if os.path.isdir("../src/aggmon"):
    sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "src")))

if __name__ == "__main__":
    from aggmon.agg_control import aggmon_control
    aggmon_control(sys.argv[1:])
